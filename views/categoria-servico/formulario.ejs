<%- include('../layouts/partials/header') %>
<% const erros = validationErrors(locals.errors); %>
<% const categoria = locals.categoria || {} %>

  <div class="page-header">
    <div>
      <h1><%= locals.titulo %></h1>
      <% if (categoria.id) { %><p>#<%= categoria.id %></p><% } %>
    </div>
    <a href="/categorias" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i> Voltar</a>
  </div>

  <div class="card" style="max-width:560px">
    <div class="card-body">
      <% if (erros.all.length) { %>
        <div class="alert alert-danger">
          <i class="bi bi-exclamation-triangle-fill me-2"></i>
          <ul class="mb-0 mt-1">
            <% erros.all.forEach(m => { %><li><%= m %></li><% }) %>
          </ul>
        </div>
      <% } %>
      <form action="/categorias/<%= categoria.id ? categoria.id + '/editar' : 'criar' %>" method="POST">
        <div class="mb-4">
          <label class="form-label">Nome da Categoria</label>
          <input name="nome" class="form-control <%= erros.has('nome') ? 'is-invalid' : '' %>"
            value="<%= categoria.nome || '' %>" placeholder="Ex: Limpeza residencial" />
          <% if (erros.has('nome')) { %>
            <div class="text-danger small"><%= erros.get('nome')[0] %></div>
          <% } %>
        </div>
        <div class="d-flex gap-2">
          <button type="submit" class="btn btn-primary">Salvar</button>
          <a href="/categorias" class="btn btn-outline-secondary">Cancelar</a>
        </div>
      </form>
    </div>
  </div>

<%- include('../layouts/partials/footer') %>
